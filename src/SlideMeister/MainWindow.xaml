<Window x:Class="SlideMeister.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SlideMeister"
        mc:Ignorable="d"
        Title="SlideMeister" Height="700" Width="900" SizeChanged="Window_SizeChanged">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80*"></ColumnDefinition>
            <ColumnDefinition Width="20*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Menu Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
            <MenuItem Header="_File">
                <MenuItem Header="Open..." Click="FileOpen_Click"></MenuItem>
                <Separator />
                <MenuItem Header="Close" Click="Close_Click"></MenuItem>
            </MenuItem>
        </Menu>
        <Canvas x:Name="BackgroundCanvas" Grid.Row="1" Grid.Column="0">
            <Menu />

        </Canvas>
        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Vertical" x:Name="StateButtons"></StackPanel>


    </Grid>

</Window>
